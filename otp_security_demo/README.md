# Context-Aware OTP Security Warning System

Minimal Flask demo for a context-aware OTP verification screen.

## Run locally

```bash
cd otp_security_demo
pip install -r requirements.txt
python app.py
```

Then open `http://127.0.0.1:5000`.
